{
  "name": "swp-backend-deploy",
  "version": "1.0.0",
  "description": "Backend service for deployment",
  "main": "backend/index.js",
  "scripts": {
    "start": "node backend/index.js",
    "install-backend": "cd backend && npm install",
    "postinstall": "npm run install-backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
